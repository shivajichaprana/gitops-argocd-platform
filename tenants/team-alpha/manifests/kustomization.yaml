apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: team-alpha
    # Label metadata only — selectors stay stable if labels change later.
    includeSelectors: false
